{
  "name": "talos-ui-site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "bun --filter showcase dev",
    "build": "bun --filter showcase build"
  },
  "engines": {
    "bun": ">=1.2.0"
  },
  "trustedDependencies": [
    "@j_shelfwood/talos-ui"
  ],
  "dependencies": {
    "@j_shelfwood/talos-ui": "^0.5.1"
  }
}
